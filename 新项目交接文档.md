# 📄 新项目交接与开发文档：全自动研报聚合与同步系统

**【背景信息】**
*   **目标仓库**：我已经将原作者的研报项目 Fork 到了我的个人账号下：`https://github.com/19138999001tf-tech/report/`
*   **本地环境**：我已经在本地新建了一个名为 `report` 的项目文件夹。
*   **核心诉求**：
    1.  **代码拉取与同步**：在本地 `report` 文件夹内把我的 Fork 仓库拉取下来，并配置好 Git 以便保持同步。
    2.  **增量自动归档 (GitHub Actions)**：实现每天中午在 GitHub 云端自动执行脚本，跨库拉取原作者 `manymore13/report` 的增量研报更新，归档并推送到我的仓库。
    3.  **构建 AI 全量知识库**：在项目中增加一个处理模块，每次增量更新后，自动将散落的多个行业 CSV 数据聚合生成一个唯一的“全量研报信息总表（如 `All_Reports_Summary.csv`）”，该总表要高度扁平化、去重，专门方便后续喂给 AI 大模型或 RAG 系统读取。

---

**【给新 Agent (AI 助手) 的执行指令】**

你好，我是用户。请仔细阅读上方背景，并严格按照以下阶段帮我完成环境配置和代码开发：

### 阶段一：本地 Git 环境初始化
1. 请帮我在当前目录下执行 `git clone https://github.com/19138999001tf-tech/report.git .` （或者拉取到合适的子目录），确保代码完整落盘。
2. 帮我配置好与我个人 GitHub 仓库的关联，确保可以直接 `push` 和 `pull`。

### 阶段二：编写“全量研报聚合”脚本
1. 在项目根目录创建一个 Python 脚本（命名为 `aggregate_all.py`）。
2. **脚本逻辑要求**：
   * 自动遍历并读取 `eastmoney/` 目录下的所有行业 `.csv` 文件。
   * 使用 `pandas` 将它们合并，处理可能存在的字符编码问题（建议 `utf-8-sig`）。
   * 按照“发布时间”倒序排列（最新的在最前），并根据“研报名称”和“研报地址”进行去重。
   * 将合并后的结果输出到根目录的 `All_Reports_Summary.csv` 文件中，确保字段清晰，方便作为 LLM 的检索数据库。

### 阶段三：配置 GitHub Actions 自动同步与聚合
1. 在项目根目录创建工作流文件 `.github/workflows/sync_and_aggregate.yml`。
2. **工作流逻辑要求**：
   * **定时触发**：每天北京时间 13:00（即 UTC 5:00）自动触发。
   * **配置上游**：将 `https://github.com/manymore13/report.git` 设置为 `upstream`，强制 `fetch` 并 `merge` 它的 `main` 分支到当前仓库，实现增量研报拉取。
   * **运行聚合**：拉取新数据后，配置 Python 3.10 环境并安装 `pandas`，运行刚才写的 `aggregate_all.py` 生成最新的全量大表。
   * **自动推送**：把新合并过来的原作者数据、以及新生成的 `All_Reports_Summary.csv`，一起 `commit` 并 `push` 进 `19138999001tf-tech/report` 仓库。

**【执行要求】**
请不要让我去网上找代码，请直接为我输出上述所需的 Python 脚本和 YAML 工作流代码，并如果需要执行本地终端命令，请直接通过你的工具帮我执行。收到指令后，请立即开始阶段一的工作！
